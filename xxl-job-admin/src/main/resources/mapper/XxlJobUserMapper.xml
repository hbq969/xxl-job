<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.job.admin.mapper.XxlJobUserMapper">

	<resultMap id="XxlJobUser" type="com.xxl.job.admin.model.XxlJobUser" >
		<result column="id" property="id" />
		<result column="username" property="username" />
	    <result column="password" property="password" />
		<result column="token" property="token" />
	    <result column="role" property="role" />
	    <result column="permission" property="permission" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.username,
		t.password,
		t.token,
		t.role,
		t.permission
	</sql>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="XxlJobUser">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_job_user AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="username != null and username != ''">
				AND t.username like CONCAT(CONCAT('%', #{username}), '%')
			</if>
			<if test="role gt -1">
				AND t.role = #{role}
			</if>
		</trim>
		ORDER BY username ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_job_user AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="username != null and username != ''">
				AND t.username like CONCAT(CONCAT('%', #{username}), '%')
			</if>
			<if test="role gt -1">
				AND t.role = #{role}
			</if>
		</trim>
	</select>

	<select id="loadByUserName" parameterType="java.util.HashMap" resultMap="XxlJobUser">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_job_user AS t
		WHERE t.username = #{username}
	</select>

	<select id="loadById" parameterType="java.util.HashMap" resultMap="XxlJobUser">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_job_user AS t
		WHERE t.id = #{id}
	</select>

	<insert id="save" parameterType="com.xxl.job.admin.model.XxlJobUser" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_job_user (
			username,
			password,
			role,
			permission
		) VALUES (
			#{username},
			#{password},
			#{role},
			#{permission}
		);
	</insert>

	<update id="update" parameterType="com.xxl.job.admin.model.XxlJobUser" >
		UPDATE xxl_job_user
		SET
			<if test="password != null and password != ''">
				password = #{password},
			</if>
			role = #{role},
			permission = #{permission}
		WHERE id = #{id}
	</update>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE
		FROM xxl_job_user
		WHERE id = #{id}
	</delete>

	<update id="updateToken" parameterType="java.util.HashMap" >
		UPDATE xxl_job_user
		SET token = #{token}
		WHERE id = #{id}
	</update>

	<sql id="Base_Group_Column_List">
		t.id,
		t.app_name,
		t.title,
		t.address_type,
		t.address_list,
		t.update_time
	</sql>

	<resultMap id="XxlJobGroup" type="com.xxl.job.admin.model.XxlJobGroup" >
		<result column="id" property="id" />
		<result column="app_name" property="appname" />
		<result column="title" property="title" />
		<result column="address_type" property="addressType" />
		<result column="address_list" property="addressList" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<select id="queryUserGroupList" resultType="com.xxl.job.admin.model.XxlJobGroup" resultMap="XxlJobGroup">
        <choose>
            <when test="roleNames.contains('ADMIN')">
                SELECT
                <include refid="Base_Group_Column_List"/>
                FROM xxl_job_group AS t
                ORDER BY t.app_name, t.title, t.id ASC
            </when>
            <otherwise>
                SELECT
                <include refid="Base_Group_Column_List"/>
                FROM xxl_job_group AS t,h_xxl_job_user_p x
                where t.id=x.group_id
                and x.user_name=#{userName}
                ORDER BY t.app_name, t.title, t.id ASC
            </otherwise>
        </choose>
    </select>

	<insert id="saveUserPermission">
        insert into h_xxl_job_user_p(user_name, group_id)
        values (#{userName}, #{groupId})
    </insert>

	<delete id="deleteUserPermission">
        delete
        from h_xxl_job_user_p
        where user_name = #{userName}
    </delete>
</mapper>