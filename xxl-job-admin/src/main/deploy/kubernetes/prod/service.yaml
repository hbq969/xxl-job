apiVersion: v1
kind: Service
metadata:
  name: xxl-job-admin
  namespace: ${k8s_ns}
  labels:
    app: xxl-job-admin
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - port: 30630
      targetPort: 30630
      protocol: TCP
      name: restful
  selector:
    app: xxl-job-admin

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: xxl-job-admin-deployment
  namespace: ${k8s_ns}
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      name: xxl-job-admin
  template:
    metadata:
      labels:
        name: xxl-job-admin
        app: xxl-job-admin
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values:
                      - suzhou
                      - wuxi
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - xxl-job-admin
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: xxl-job-admin
      tolerations:
        - key: node.pool.kubernetes.io
          operator: Equal
          value: p15f1
          effect: NoSchedule
      restartPolicy: Always
      volumes:
        - name: app-logs
          hostPath:
            path: /tmp/apps/xxl-job-admin/logs
      containers:
        - name: xxl-job-admin
          image: ${docker_prefix}/xxl-job-admin:${tag}
          imagePullPolicy: Always
          livenessProbe:
            tcpSocket:
              port: 30630
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 240
          readinessProbe:
            tcpSocket:
              port: 30630
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 240
          resources:
            requests:
              cpu: '1'
              memory: 1Gi
            limits:
              cpu: '1'
              memory: 1.5Gi
          volumeMounts:
            - mountPath: /opt/app/xxl-job-admin/logs
              name: app-logs
          ports:
            - containerPort: 30630
          env:
            - name: spring_cloud_zookeeper_enabled
              value: "false"
            - name: spring_cloud_zookeeper_connectString
              value: ""
            - name: spring_cloud_zookeeper_auth_info
              value: ""
            - name: spring_profiles_active
              value: "${spring_profiles_active}"
          command: [ "java" ]
          args: [ "-Xmx512m","-Xms512m","-DAPP_SN=h-dbc","-Dspring.profiles.active=${spring_profiles_active}", "-Dspring.main.allow-bean-definition-overriding=true","-Dreactor.netty.http.server.accessLogEnabled=true","-Dfile.encoding=utf-8","-XX:+UseG1GC","-XX:MaxGCPauseMillis=100", "-jar","/opt/app/xxl-job-admin/lib/xxl-job-admin-3.2.0.jar" ]